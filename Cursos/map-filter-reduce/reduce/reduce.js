// EXECUTA UMA FUNÇÃO EM TODOS OS ELEMENTOS DO ARRAY,
// RETORNANDO UM VALOR ÚNICO
// array.reduce(callbackFn, initialValue);

// SINTAXE
const callbackFn = function(accumulator, curentValue, index, array) {
    // do something
}

array.reduce(callbackFn, initialValue);